//handles renderer messages
const { ipcMain } = require('electron');
//const { askAI } = require('./aiService');

/*ipcMain.handle('ask-ai', async (event, question) => {
  return await askAI(question);
});

*/