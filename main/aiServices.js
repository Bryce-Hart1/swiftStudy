//makes api calls to AI
/* (async function askAI(question) {
  // Simulate delay
  await new Promise(r => setTimeout(r, 500));
  return `AI says: "${question}" sounds interesting!`;
}

module.exports = { askAI };

*/
